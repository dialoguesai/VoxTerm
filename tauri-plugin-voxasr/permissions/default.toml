"$schema" = "schemas/schema.json"

[default]
description = "Allows VoxTerm to start/stop on-device transcription and poll its transcript."
permissions = ["allow-start-transcribe", "allow-stop-transcribe", "allow-poll-transcript"]
